#include <string>
#include <vector>

std::string all_names(std::vector<std::string> some_names);
const char *all_names_c_strings(const char *some_names[]);   // In the starter files, this is TOTALLY BROKEN

